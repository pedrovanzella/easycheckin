<td><%= activity.owner ? activity.owner.email : "Admin" %></td>
<td>Editou assento 
<% if activity.trackable %>
    <%= activity.trackable.row %><%= activity.trackable.number %>
<% else %>
    que já foi removido
<% end %>
</td>
<td><%= activity.created_at %></td>