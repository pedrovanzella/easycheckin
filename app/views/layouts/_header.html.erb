<nav class="navbar navbar-dark bg-inverse">
  <div class="container-fluid">
    <div class="row">
      <div class="col-md-3">
        <%= link_to "Home", root_path, class: "navbar-brand" %>

        <ul class="nav navbar-nav navbar-nav-left">

          <li class="nav-item">
            <%= link_to 'About', page_path('about'), class: 'nav-link' %>
          </li>
        </ul>
      </div>

      <div class="col-md-4">
        <ul class="nav navbar-nav">
          <% if current_user %>
            <li class="nav-item">
              <%= link_to 'Passagens', tickets_path, class: 'btn btn-default navbar-btn' %>
            </li>
            <li class="nav-item">
              <%= link_to 'Vôos', flights_path, class: 'btn btn-default navbar-btn' %>
            </li>
          <% end %>
        </ul>
      </div>

      <div class="col-md-4">
        <ul class="nav navbar-nav navbar-right">
          <% if current_user %>
            <li class="nav-item">
              <%= link_to 'Sair', destroy_user_session_path, method: :delete, class: 'btn btn-default navbar-btn' %>
            </li>
          <% else %>
            <li class="nav-item">
              <%= link_to 'Entrar', new_user_session_path, class: 'btn btn-default navbar-btn' %>
            </li>
            <li class="nav-item">
              <%= link_to 'Registrar', new_user_registration_path, class: 'btn btn-default navbar-btn' %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</nav>
